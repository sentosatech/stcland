# Use a specific registry for all packages
registry=https://registry.npmjs.org/

# Authentication token for publishing packages
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Optionally, configure a specific scope for the organization
@stcland:registry=https://registry.npmjs.org/

# Ensure workspace protocol is properly handled
save-workspace-protocol=true