name: Chromatic Deployment

on:
  pull_request:
    paths:
      - 'packages/storybook/**'
      - '.github/workflows/chromatic.yml'

jobs:
  chromatic-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository with full history
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # Setup Node.js and pnpm
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install pnpm
        run: npm install -g pnpm

      # Install dependencies
      - name: Install dependencies
        run: pnpm install

      # Build @stcland/components before Storybook build
      - name: Build @stcland/components
        run: pnpm --filter @stcland/components build

      # Run Chromatic
      - name: Run Chromatic
        working-directory: packages/storybook
        run: pnpm chromatic --project-token=${{ secrets.CHROMATIC_PROJECT_TOKEN }} --build-script-name build-sb