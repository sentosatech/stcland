import { Canvas, Meta } from '@storybook/blocks';
import * as RadioButtonStories from './InputRadioButton.stories';

<Meta of={RadioButtonStories} />
# RadioButtonGroup Component

The `RadioButotnGroup` component is used for plain text form inputs.

## Properties
- **name** (`string`): Required. The input elements name, this will become the key of the input on form submission. Can be nested using dot notation ie (fieldset.email)
- **id** (`string`): Nullable. used to tag the input element with an Id. must be unique
- **label** (`string`): Nullable. label text that appears on top of the input element
- **required** (`boolean`): Nullable. used to determine if field is required.
- **disabled** (`boolean`): Nullable. used to determine if field is disabled.
- **hidden** (`boolean`): Nullable. used to determine if field is hidden. Can be used to have prefilled content without being visible to the end user
- **direction** (`function`): Nullable. used determine if radio buttons are vertical or horizontal
- **radioButtons** (`Array<{value: string, label: string}>`): Required. options for the radio button

### Variants:
#### Default
<Canvas of={RadioButtonStories.Default} />
#### Disabled
<Canvas of={RadioButtonStories.Disabled} />
#### Required
<Canvas of={RadioButtonStories.Required} />
#### Hidden
<Canvas of={RadioButtonStories.Hidden} />
#### Vertical
<Canvas of={RadioButtonStories.Vertical} />
#### WithStyles
<Canvas of={RadioButtonStories.WithStyles} />
---
