import { Canvas, Meta } from '@storybook/blocks'
import * as ListStories from './List.stories'

<Meta of={ListStories} />

# List Components

The `List` component provides a flexible way to render a list of items, either ordered or unordered. It allows customization of padding, gutters, and density, making it easy to adapt to different design requirements.

---

## Properties

### List
- **children** (`React.ReactNode`): The list items or subcomponents rendered within the list.
- **className** (`string`): Additional classes for styling.
- **dense** (`boolean`): If `true`, reduces the padding between list items. Defaults to `false`.
- **disablePadding** (`boolean`): If `true`, disables the default padding for the list. Defaults to `false`.
- **disableGutters** (`boolean`): If `true`, removes the horizontal padding from list items. Defaults to `false`.
- **subheader** (`React.ReactNode or string`): A subheader element or string rendered above the list items.
- **ordered** (`boolean`): If `true`, renders an ordered list (`<ol>`), otherwise an unordered list (`<ul>`). Defaults to `false`.
- **divider** (`boolean`): If `true`, adds a divider line below the list. Mostly used when there are nested lists. Defaults to `false`.
---

### ListItem
- **alignItems** (`'center' | 'start'`): Defines the alignment of the list item content. Defaults to `'center'`.
- **children** (`React.ReactNode`): The content of the list item.
- **className** (`string`): Additional CSS classes for styling.
- **divider** (`boolean`): If `true`, adds a divider line below the list item. Defaults to `false`.

---

### ListItemButton
- **alignItems** (`'center' | 'start'`): Defines the alignment of the button content. Defaults to `'center'`.
- **autoFocus** (`boolean`): If `true`, focuses the button automatically when the component mounts. Defaults to `false`.
- **children** (`React.ReactNode`): The content of the button.
- **className** (`string`): Additional CSS classes for styling.
- **dense** (`boolean`): If `true`, applies dense styles to the button. Overrides the `List` context. Defaults to `false`.
- **disabled** (`boolean`): If `true`, disables the button. Defaults to `false`.
- **disableGutters** (`boolean`): If `true`, disables horizontal padding. Overrides the `List` context. Defaults to `false`.
- **divider** (`boolean`): If `true`, adds a divider above the button. Defaults to `false`.
- **selected** (`boolean`): If `true`, styles the button as selected. Defaults to `false`.

---

### ListItemText
- **children** (`React.ReactNode`): Additional content to render inside the text block.
- **className** (`string`): Additional CSS classes for styling.
- **inset** (`boolean`): If `true`, applies inset styling. Defaults to `false`.
- **primary** (`React.ReactNode`): The primary text content.
- **secondary** (`React.ReactNode`): The secondary text content.

---

## Examples

### Default List

This example shows a basic unordered list with default styles and props.

<Canvas of={ListStories.Default} />

---

### Ordered List

This example demonstrates rendering an ordered list with some items.

<Canvas of={ListStories.Ordered} />

---

### List Inset

This example demonstrates a list with inset behavior/style.

<Canvas of={ListStories.WithInset} />

---

### List with Dividers

This example shows a list where each item has a divider.

<Canvas of={ListStories.WithDivider} />

---

### List with Buttons

This example demonstrates a list with buttons and ListItemText element, when the property `disabled` is passed, it will apply a style to the child.

<Canvas of={ListStories.WithButtons} />

---

### List with Nested List

<Canvas of={ListStories.NestedWithToggleExample} />

---

### With Selected Item

<Canvas of={ListStories.WithSelectedItem} />

---

### With Custom Styles

If you want to style a single instance, you can explicitly pass `customStyles` to the parent `List` without importing `List` from `withStyles`. 
Note: This approach is not bound under `StcStylesProvider`, so changes in `stc.config.ts` will not affect these one-time customizations.

<Canvas of={ListStories.WithCustomStyles} />

----

### WithStyles Wrapper

When a component is exported from the `@stcland/components/withStyles` module and wrapped in the `StcStylesProvider` it allow 
users to access styled versions of the core components. These components enhance the base functionality with added styling without
the need to pass a `customStyles` property as the **With Custom Styles** example above.

<Canvas of={ListStories.WithStylesWrapper} />